	[db]  <--------------> [db client/db process]

if the password is in file on the db client or is in the db process software
itself  machine it must be accessible by the db process
to pick it up. 

	In all these situations a hacker, if he can hack the db client machine
	can pick up the password.
	It doesnt matter if the password is encrypted because the decrytion
	key must be on the machine also.
	changing from password to private key does not help.
	(helps a little)

	Why is having the db password/private key in an environment variable better?
	(not written to ANY file)
	If Im root I can see env variables (and all memory!). Game over.
	If im not root.
	If im not the same user running the software, I CANNOT see the environment variables.
	if I am the user that is running the software there are still several ways to do it:
	1. Install a more secure kernel that does not have /proc/[pid]/envrion
	2. remove the password from the env variables once you use it.
