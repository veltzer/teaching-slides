flowchart TD
    start[Request] --> check{Check counter}
    check --> |Under limit| inc[Increment counter]
    check --> |Over limit| reject[Reject request]
    inc --> allow[Allow request]

    subgraph Redis
    counter[INCR request:ip:counter]
    expiry[EXPIRE request:ip:counter 60]
    end

    inc --> counter --> expiry