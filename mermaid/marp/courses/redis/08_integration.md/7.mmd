graph LR
    subgraph "Job Producer"
        API[API Server]
    end

    subgraph "Redis Queue"
        List[Redis List]
    end

    subgraph "Workers"
        W1[Worker 1]
        W2[Worker 2]
        W3[Worker 3]
    end

    API -->|RPUSH jobs| List
    List -->|BLPOP jobs| W1
    List -->|BLPOP jobs| W2
    List -->|BLPOP jobs| W3