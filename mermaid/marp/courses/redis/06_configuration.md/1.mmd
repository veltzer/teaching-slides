graph TD
    subgraph "Essential Parameters"
        bind["bind (network interfaces)"]
        port["port (default: 6379)"]
        requirepass["requirepass (authentication)"]
        maxmemory["maxmemory (memory limit)"]
        maxmemory-policy["maxmemory-policy (eviction)"]
        persistence["persistence (RDB/AOF settings)"]
        timeout["timeout (client connection)"]
        tcp-keepalive["tcp-keepalive (connection health)"]
    end