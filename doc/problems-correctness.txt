How can we know if we overflow a slide and error or divide into 2 slies if that is the case?
  This is for textual slides.

How can we know if a diagram (svg) overflows a slide and resize it (?) to fit the slide or error
if that is the case.
