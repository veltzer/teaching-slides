- get back to running mdl on all slides.

- do right depenencies on mermaid slides in the makefile.

- translate all odp slides to marp.
- add support for beamer in this repo and start writing presentations in beamer.
- make this repo public

- integrate all stuff in staging.

- turn all the text syllabi into md (all files in txt/syllabi).
- make -j 8 doesn't work with the odp to ppt and pdf conversions.
	try to make it work.
- try to use asciidoc and a2x for my slides.

- write slides and examples about tuning the glibc malloc library.
	use the information from the examples of glibc malloc in the demos-linux repo.
- make a web site showing all of my slides using gh-pages
	and embedding pdf in web pages.
- make automatic upload to slideshare and scribd.
- write slides about room for optimization.
- write slides about allocating memory fast.
- write slides about patterns.
	start with the proactor pattern.
- write slides about restrict.
- padding of structures and aligning structures in gcc and intel.
	with examples.
- the intel compiler:
	- how to install it on Linux.
	- comparison of intel compiler to gcc
		- performance examples (already have one)
	- compatibility of the intel compiler to the gcc compiler
		- do example of C++ compatibility for the intel compiler.
