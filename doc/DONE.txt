- add generation of mermaid images from mermaid source code (get that from demos-marp).
- have geneation of pdfs embed those images into the pdf.
	remembed to have marp pdfs dependent on the images so that the images will get built first.
- add number of slides in the snipplet that talks about this project.
- unite courses in marp/courses into a single pdf using the unite.sh script using the makefile.
