PCONFIG = {
    "pyclassifiers",
}
PBUILD = {
    "pyclassifiers",
    "pydmt",
    "pymakehelper",
    "pycmdtools",
}
PTEST = {
    "pylint",
    "pytest",
    "mypy",
    "ruff",
}
PDEPS = {
    "ripgrep",
}
CONFIG = {}
BUILD = {
    "pyclassifiers",
    "pydmt",
    "pymakehelper",
    "pycmdtools",
}
TEST = {
    "pylint",
    "pytest",
    "mypy",
    "ruff",
}
DEPS = {}

return {
    PCONFIG = PCONFIG,
    PBUILD = PBUILD,
    PTEST = PTEST,
    PDEPS = PDEPS,
    CONFIG = CONFIG,
    BUILD = BUILD,
    TEST = TEST,
    DEPS = DEPS,
}
